# Simulations (generated output)

**Sources:** [`work/notebooks/mathematical-models/`](../../work/notebooks/mathematical-models/) (`trust-dynamics.ipynb`, `cultural-adaptation.ipynb`).

**Regenerate:** from `j-adezhao/` run `./scripts/run-simulations.sh` (needs Jupyter; see [`scripts/setup-env.sh`](../../scripts/setup-env.sh)).

**Git:** `executed-notebooks/`, `simulation-run-*.txt`, and `*.csv` here are **ignored** so the repo stays small. Outputs stay on your machine after you run the script.
